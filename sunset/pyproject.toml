[project]
name = "sunset"
version = "0.1.0"
description = "Legacy script modernization agent that proves its own work"
requires-python = ">=3.10"
dependencies = [
    "daytona>=0.204.0",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
